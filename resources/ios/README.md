# iOS App Icons

Generated by `npm run icons:ios` from root `icon.svg`.

After `npx cap add ios`, copy this folder into the Xcode project:

```
resources/ios/AppIcon.appiconset → ios/App/App/Assets.xcassets/AppIcon.appiconset
```

Or re-run `npm run icons:ios` before each `npm run cap:sync`.
